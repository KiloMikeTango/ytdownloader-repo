- name: Build and Index Package
        run: |
          sudo apt-get update && sudo apt-get install -y dpkg-dev
          
          # 1. Setup the standard Debian structure
          mkdir -p build/dists/stable/main/binary-all
          
          # 2. Build the package
          dpkg-deb --build package build/dists/stable/main/binary-all/ytdownloader_1.0.0_all.deb
          
          # 3. Scan packages (Point to the binary-all folder)
          cd build/dists/stable/main
          dpkg-scanpackages binary-all/ > Packages
          gzip -c Packages > binary-all/Packages.gz
          
          # 4. Create Release file (Inside dists/stable/)
          cat <<EOF > Release
          Origin: ytdownloader-repo
          Label: ytdownloader
          Suite: stable
          Codename: stable
          Architectures: all
          Components: main
          Description: YtDownloader Repository
          EOF
          
          # 5. Move install script to the root
          cd ../../../..
          cp install.sh build/install.sh
